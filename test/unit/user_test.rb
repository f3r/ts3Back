require 'test_helper'

class UserTest < ActiveSupport::TestCase

  # test "should authenticate with matching username and password" do
  #   user = Factory(:user, :email => 'user@example.com', :password => 'secret')
  #   Devise.warden.authenticate!(:email => 'user@example.com', :password => 'secret').should == user
  # end
  # 
  # test "should not authenticate with incorrect password" do
  #   user = Factory(:user)
  #   Devise.warden.authenticate!(:email => 'user@example.com', :password => 'el_secret').should == user
  # end

end
